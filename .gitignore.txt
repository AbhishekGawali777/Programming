# 1. Ignore everything by default
*

# 2. Ignore the build directory and its contents entirely
build/

# 3. Allow directories (required to find files inside other folders)
!*/

# 4. Allow specific file types
!*.cpp
!*.h
!.gitignore
!CMakeLists.txt
